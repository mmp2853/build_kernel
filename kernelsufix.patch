--- a/drivers/kernelsu/manager/pkg_observer.c
+++ b/drivers/kernelsu/manager/pkg_observer.c
@@ -21,11 +21,11 @@
 
 static struct fsnotify_group *g;
 
-static int ksu_handle_inode_event(struct fsnotify_mark *mark, u32 mask, struct inode *inode, struct inode *dir,
-                                  const struct qstr *file_name, u32 cookie)
+static int ksu_handle_event(struct fsnotify_group *group, struct inode *inode, u32 mask, const void *data,
+                            int data_type, const struct qstr *file_name, u32 cookie, struct fsnotify_iter_info *iter_info)
 {
     if (!file_name)
         return 0;
     if (mask & FS_ISDIR)
         return 0;
     if (file_name->len == 13 && !memcmp(file_name->name, "packages.list", 13)) {
@@ -36,7 +36,7 @@
 }
 
 static const struct fsnotify_ops ksu_ops = {
-    .handle_inode_event = ksu_handle_inode_event,
+    .handle_event = ksu_handle_event,
 };
 
 static int add_mark_on_inode(struct inode *inode, u32 mask, struct fsnotify_mark **out)

--- a/drivers/kernelsu/runtime/ksud_integration.c
+++ b/drivers/kernelsu/runtime/ksud_integration.c
@@ -10,6 +10,18 @@
 #include "klog.h" // IWYU pragma: keep
 #include "manager/throne_tracker.h"
 
+#include <linux/version.h>
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 10, 0)
+#ifndef copy_from_user_nofault
+#define copy_from_user_nofault(dst, src, size) probe_kernel_read(dst, src, size)
+#endif
+#ifndef copy_to_user_nofault
+#define copy_to_user_nofault(dst, src, size) probe_kernel_write(dst, src, size)
+#endif
+#endif
+
 int ksud_init(void)
 {
     return 0;

--- a/drivers/kernelsu/hook/arm64/patch_memory.c
+++ b/drivers/kernelsu/hook/arm64/patch_memory.c
@@ -10,6 +10,13 @@
 #include <asm/memory.h>
 #include <asm/smp_plat.h>
 
+#include <linux/version.h>
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 10, 0)
+#ifndef copy_to_kernel_nofault
+#define copy_to_kernel_nofault(dst, src, size) probe_kernel_write(dst, src, size)
+#endif
+#endif
+
 struct patch_context {
 	void *addr;
 	void *data;
